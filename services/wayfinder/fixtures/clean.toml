# EA-4 positive fixture — mirrors the real services/wayfinder/Cargo.toml dependency set. Must pass
# (exit 0) both before and after EA-4: no name here is anywhere close to FORBIDDEN.
[package]
name = "audit-fixture-clean"
version = "0.0.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
